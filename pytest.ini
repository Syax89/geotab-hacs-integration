[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
norecursedirs = .git .openclaw dev_venv
# Ignore lingering threads from HA core shutdown in CI
filterwarnings =
    ignore:.*_run_safe_shutdown_loop.*:pytest.PytestUnraisableExceptionWarning
